
/* document.getElementById('search-unput').addEventListener('click', () => {
  window.history.pushState(null, null, `/search?q=${document.getElementById('search-input').value}`);
  console.log('1');
  router();
}); */

/* searchButton.addEventListener('onkeydown', () => {
  if (searchInput.value.length > 0) {
    searchButton.disabled = false;
  } else {
    searchButton.disabled = true;
  }
}); */
